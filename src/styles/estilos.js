import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    contenedorGral: {
        flex: 1,
    },
    contenedor: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#fff',
    },
});
